# 2026-09-20

## 朗读（Read Aloud）文字同步 + legadoT 风格设置项
- 参考 legadoT（D:\My_Project\legadoT-master）实现听书与文字同步：朗读句子现在携带来源段落 DOM id（`ReadAloudQueueItem(text, paragraphId)`，来自 `hoshiReaderPageTranslation.collectVisibleTargets()` 的 target id）。
- 同步链路：`ReadAloudController` 播到某句 → `ReadAloudState.currentParagraphId` → ReaderWebView `LaunchedEffect` → `hoshiReaderPageTranslation.highlightReadAloudTarget(id, reveal)`（新增于 reader-translation.js，段落加 `hoshi-read-aloud-active` class，用 `hoshiReader.scrollToRange` 滚动，CSS 在 reader.css）。段落级高亮（与 legado 整段染色一致），不做句内 range 映射。
- 队列播完：Controller 发 `queueExhausted` SharedFlow（不再直接 stop），ReaderWebView 收到后 `paginate('forward')`（limit 则 stop），delay 后重新 collectVisibleTargets 并 `continueWith()` 续播 —— 跨页跟读；章节末尾靠 paginate 返回 limit 停止。朗读会话退出靠 ReaderWebView `DisposableEffect` 的 stop()。
- 新增设置（ReadAloudSettings/DataStore + ReadAloudSettingsSheet 开关）：ignoreAudioFocus（Controller 跳过焦点申请与响应）、pauseWhilePhoneCalls（Controller 内 TelephonyManager：API31+ TelephonyCallback / 低版本 PhoneStateListener，需 READ_PHONE_STATE 运行时权限，ringing/offhook 暂停 idle 恢复）、wakeLock（ReadAloudService PARTIAL_WAKE_LOCK，mediaSession 停止时释放）、mediaButtonPerNext（skip 按段跳转，paragraphBoundaryIndex）、readAloudByPage（整页文本作单个 utterance、翻页 delay 600ms）。另有系统 TTS 设置入口（`com.android.settings.TTS_SETTINGS`）。
- 截图中未实现项：流式播放（仅在线 TTS 源有效，Hoshi 无在线 TTS）、系统媒体控件兼容（Hoshi 通知本就 setMediaSession(token)，恒定生效）、多角色朗读/AI 服务（无 AI 音色基础设施）。
- 提交 873de54（连同上一会话未提交的本地 TTS/前台服务/设置界面等朗读 v1 工作）。`:app:assembleDebug` 通过。
- 坑：PowerShell 下 `git commit` 多个 `-m` 各自必须单行，含换行会被拆成未知参数。
